一、什么是闭包？
  闭包即能够读取其它函数内部变量的函数，也可以把闭包简单的理解成“定义在一个函数内部的函数”。

二、闭包的用途
  它的最大用处有两个：
	1、可以读取函数内部的变量
	2、是让变量始终保持在内存中.

三、使用闭包注意点
  （1）由于闭包会使得函数中的变量都被保存在内存中，内存消耗很大，所以不能滥用闭包，否则会造成网页的性能问题,在IE中可能导致内存泄露。解决方法是，在退出函数前，将不使用的局部变量全部删除。

  （2）闭包会在父函数外部改变父函数内部的变量的值。所以，如果你把父函数当作对象（object）使用，把闭包当作它的公用方法（Public Mthod），这时一定要小心，不要随便改变父函数内部变量的值。

四、闭包的优点：
	1、不增加额外的全局变量
	2、执行过程中所有变量都在匿名函数内部

五、闭包的缺点：
	1、闭包会在父函数外部，改变父函数内部变量的值，一定要小心
	2、闭包会使得内存消耗有可能会很大，可能导致内存泄露























-134				0      正值  if(iTarget>speed){正值}else{负值}

0			-134		负值



















